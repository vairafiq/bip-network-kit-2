name: 🚀 Deploy to All Domains (Batch Upload)

on: 
  push:
    branches:
      - master

jobs:
  web-deploy:
    name: 🎉 Deploy to all domains
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v3

      - name: 🛠 Install lftp
        run: |
          sudo apt-get update
          sudo apt-get install -y lftp

      - name: 📄 Read domain list
        id: domains
        run: |
          echo "::set-output name=list::$(cat .github/domains.txt | tr '\n' ' ')"

      - name: 🛠 Deploy to each domain
        run: |
          for domain in ${{ steps.domains.outputs.list }}
          do
            echo "Deploying to $domain"
            lftp -e "
              mirror -R ./ /domains/$domain/public_html/wp-content/plugins/bip-network-kit/
              bye
            " -u ${{ secrets.FTP_USER }},${{ secrets.FTP_PASS }} ${{ secrets.FTP_SERVER }}
          done
